Pull latest code from git and start the Sentosa CMS dev server locally.

Steps to follow in order:

1. Run `git pull` to fetch and merge the latest changes from the remote repository. Show the output so the user can see what was updated.

2. Run `npm run dev` in the background using the `run_in_background: true` parameter on the Bash tool. The command to run is: `npm run dev`

3. After starting, tell the user:
   - The server is running at http://localhost:3000
   - How to stop it: use `/stopsentosa`

Working directory for all commands: `c:\Users\huy.duong\OneDrive - BnK Solutions JSC\Project\Sentosa\CODE`
